/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['https://qr-code-generator-mocha-beta.vercel.app/'],
  },
};

export default nextConfig;
